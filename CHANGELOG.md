# Changelog

This project is pre-1.0. Until a tagged release process is established, entries are tracked under **Unreleased**.

## Unreleased

### Added
- Windows and macOS CI lanes plus plugin artifact jobs (Unity/Unreal ThirdParty packaging).

### Changed
- Unity runtime now wires Unity allocator + logging by default (configurable).
- Unreal plugin forwards Astral native logs to UE logging.

